{
  "name": "rag-art",
  "version": "1.0.0",
  "description": "RAG Art Application with backend and frontend",
  "scripts": {
    "start": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "build": "echo 'No build step required for backend'"
  },
  "engines": {
    "node": "18.x"
  }
}